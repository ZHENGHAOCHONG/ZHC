<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simplenewsystem;">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".regsiter"
            android:exported="true">
        </activity>

        <activity android:name=".System"
            android:exported="true"
            android:label="模式选择">
        </activity>

        <activity android:name=".Auto"
            android:exported="true"
            android:label="自动控制模式"
            >
        </activity>

        <activity android:name=".Manual"
            android:exported="true"
            android:label="手动控制模式">
        </activity>

        <activity android:name=".JianCe"
            android:exported="true"
            android:label="实时路径跟踪"
            >
        </activity>

        <activity android:name=".PathShow"
            android:exported="true"
            android:label="实时路径跟踪"
            >
        </activity>


    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>